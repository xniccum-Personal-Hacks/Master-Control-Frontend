import React from 'react';

let anImage = require('../images/an.png');

class Header extends React.Component {
    render() {
        return (
            <header className="header">
            </header>
        );
    }
}

export default Header;